{% extends 'base.html.twig' %}

{% block body %}
    <h1>Caixa - Editar</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <div class="btn-group" role="group">
            <input class="btn-success btn btn-salvar" type="submit" value="Salvar" />
                            <a class="btn btn-primary btn-voltar" href="{{ path('caixa_index') }}">Back to the list</a>        <a class="btn btn-danger btn-excluir" href="{{ path('caixa_show', { 'id': caixa.id }) }}">Excluir</a></div>
    {{ form_end(edit_form) }}


{% endblock %}
